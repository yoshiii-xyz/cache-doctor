# Fixtures

The integration tests construct temporary cache trees so the repository has
no generated registry state or credential material. Keep any committed
fixture metadata synthetic and free of private paths or secret values.
