# Changelog

## 0.1.0

- Added a bounded, read-only Cargo cache scanner.
- Added artifact, index metadata, lockfile, source replacement, credential
  presence, checksum, yanked, stale-index, and malformed-cache findings.
- Added deterministic JSON and text reports, CLI integration tests, and fuzz
  coverage for report decoding.
